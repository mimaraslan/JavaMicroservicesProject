server:
  port: 9594

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://127.0.0.1:9411/api/v2/spans

spring:

  datasource:
    #url: jdbc:mysql://localhost:3306/micro_notificationservicedb   # MySQL veritabanı URL'si
    #username: root                                    # Veritabanı kullanıcı adı
    #password: 123456789                                  # Veritabanı şifresi
    #driver-class-name: com.mysql.cj.jdbc.Driver            # MySQL JDBC sürücüsü
  
    url: jdbc:postgresql://localhost:9999/Micro_NotificationServiceDB   # PostgreSQL veritabanı URL'si (Docker)
    username: postgres                                    # Veritabanı kullanıcı adı
    password: 123456789                                  # Veritabanı şifresi
    driver-class-name: org.postgresql.Driver             # PostgreSQL JDBC sürücüsü
  jpa:
    hibernate:
      ddl-auto: update                                 # Veritabanı şemasının otomatik güncellenmesi
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
#    producer:
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: notification-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

  mail:
    host: smtp.gmail.com
    port: 587
    username: mimaraslan@gmail.com
    password: eqme snej egez mcsb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

springdoc:
  swagger-ui:
    path: /swagger-ui.html




